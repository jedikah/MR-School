import {CreateAnneeScolaireResolver} from "./create-annee-scolaire.resolver";
import {UpdateAnneeScolaireResolver} from "./update-annee-scolaire.resolver";

export const AnneeScolaireResolvers = [
    CreateAnneeScolaireResolver,
    UpdateAnneeScolaireResolver
]
