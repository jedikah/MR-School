import {CreateClasseResolver} from "./create-classe.resolver";
import { ListClassResolver } from './list-class.resolver';

export const ClasseResolvers = [
    CreateClasseResolver,
    ListClassResolver
]
